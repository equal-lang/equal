<em style="display:inline;"><strong>Equal Paper</strong></em>
{{#each tools}}
{{#if this.name}}
<button id="tool-{{this.name}}" class="button-{{this.class}}">
  {{#if this.display}}
  {{this.display}}
  {{else}}
  {{this.name}}
  {{/if}}
</button>
{{/if}}
{{/each}}
<span id="toolbar-file"></span>
