{
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Program",
      "skipFiles": [
        "<node_internals>/**"
      ],
      "program": "${workspaceFolder}/cli-build/equal/cli.js",
      "args": ["${workspaceFolder}/test/fib.eq"],
      // "outFiles": [
      //   "${workspaceFolder}/cli-build/*.js"
      // ]
    }
  ]
}