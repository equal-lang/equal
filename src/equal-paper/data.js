class EditorData {
  constructor() {
    console.log(document);
  }
}

export { EditorData };

// define variables in load.js and pass them into dom
// setup toolbar?
// isvisible?
  // similarly depend on
// till hidehtml